#!/bin/bash
# jllm — thin wrapper around the local-llm fat JAR
set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
JAR="$SCRIPT_DIR/target/local-llm.jar"

if [ ! -f "$JAR" ]; then
    echo "Error: $JAR not found. Run './build.sh' first." >&2
    exit 1
fi

exec java -jar "$JAR" "$@"
